import styled from "styled-components"

const CloseIcon = styled.div`
  position: absolute;
  top: 20px;
  right: 20px;
`

const ModalContent = styled.div`
  margin-top: 24px;
`

export { CloseIcon, ModalContent };