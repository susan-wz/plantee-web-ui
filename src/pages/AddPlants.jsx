import { Layout } from "../blocks"
import { Icon } from "../elements"

export default function AddPlants() {
  return (
    <Layout settings back title="Add Plants">
      <Icon symbol="takePhoto" background size={48} colour="white" />
    </Layout>
  );
}